{
	"name": "@pplx/api",
	"version": "0.2.0",
	"description": "Perplexity API SDK for Node and Bun",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"files": [
		"dist",
		"src",
		"README.md"
	],
	"scripts": {
		"build": "bun build src/index.ts --outdir dist --target node --format esm && tsc -p tsconfig.build.json",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"eventsource-parser": "^3.0.6",
		"fast-json-patch": "^3.1.1"
	}
}
